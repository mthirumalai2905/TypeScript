"use strict";
let myName = 'thiru';
//inference - mean obvious typescript automatically 
//it automatically understands that datatype we dont have to mention it
let myFavNum = true;
// myFavNum = 32
let firstName = 'thiru'; // The compiler infers the type string for the variable name
let age = 25; //The compiler infers the type number from the variable name
let isLoggedIn = true; //The compiler infers the type boolean from the variable name 
